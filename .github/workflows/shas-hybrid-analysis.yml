name: Shas hybrid city and ballot analysis

on:
  push:
    paths:
      - 'research/analyze_shas_hybrid_ballots.py'
      - '.github/workflows/shas-hybrid-analysis.yml'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --quiet numpy pandas requests scipy
      - name: Run hybrid analysis
        run: python research/analyze_shas_hybrid_ballots.py
      - name: Upload analysis files
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: shas-hybrid-analysis
          path: research/hybrid_output/
          if-no-files-found: warn
          retention-days: 7
